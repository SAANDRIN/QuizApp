<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Estilo base com as customizações -->
    <style name="Base.Theme.AppQuiz" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize seu tema base aqui -->
        <item name="colorPrimary">@color/primaria</item>
        <item name="colorPrimaryVariant">@color/primaria_variacao</item>
        <item name="android:windowBackground">@color/fundo</item>
    </style>

    <!-- Tema principal herda do base -->
    <style name="Theme.AppQuiz" parent="Base.Theme.AppQuiz" />

</resources>
